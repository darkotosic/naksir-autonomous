# HTTP klijenti
requests>=2.32.0
httpx>=0.27.0

# OpenAI novi SDK – potreban za Responses API (in-depth analysis)
openai>=1.40.0

# Telegram (ako koristiš send preko HTTPX onda OK, ali bolje da bude instalirano)
python-telegram-bot>=20.7

# Upravljanje vremenom / timezones
python-dateutil>=2.9.0

# FastAPI minimalno (backend koristiš za health-check ili API endpoint)
fastapi>=0.110.0
uvicorn>=0.29.0

# JSON schema / sigurnije parsiranje
pydantic>=2.6.0

# Cron / scheduling — GitHub Actions radi scheduling, ali lokalno treba ovo
schedule>=1.2.1

# Utility libraries
numpy>=1.26.0
